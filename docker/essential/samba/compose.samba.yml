services:
  samba:
    image: dockurr/samba:latest
    container_name: samba
    restart: always

    ports:
      - "445:445/tcp"

    volumes:
      - /devDisk:/devDisk
      - /userDisk:/userDisk
      - /opt/samba/users.conf:/etc/samba/users.conf
      - /opt/samba/smb.conf:/etc/samba/smb.conf

    networks:
      devNetwork:
        ipv4_address: 172.20.0.5

networks:
  devNetwork:
    external: true
    
# docker compose -p essential -f compose.samba.yml up -d --build 


