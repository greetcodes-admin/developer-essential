---
services:
  qbittorrent:
    image: lscr.io/linuxserver/qbittorrent:latest
    container_name: qbittorrent
    hostname: qbittorrent

    environment:
      - TZ=Asia/Kolkata
      - WEBUI_PORT=8080
      - TORRENTING_PORT=6881

    volumes:
      - /opt/qbittorrent:/config
      - /devDisk/downloads:/downloads

    # ports:
    #   - "8080:8080/tcp" # WebUI
    #   - "6881:6881/tcp" # tcp connection port
    #   - "6881:6881/udp" # udp connection port
      
    restart: unless-stopped

    networks:
      devNetwork:
        ipv4_address: 172.20.0.241

networks:
  devNetwork:
    external: true

# docker compose -p other -f compose.bittorrent.yml up -d --build  